<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Game</title>
    <script type="text/javascript" src="/js/index.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body>
    <div class="container my-5">
        <h1 class="text-center mb-4">Edit Game Details</h1>

        <div class="edit-game-section">
            <form method="post" action="/updateGame">
                <input name="app_id" type="hidden" value="<%= game.app_id %>">
                <div class="mb-3">
                    <label for="name" class="form-label">Game Name</label>
                    <input type="text" class="form-control" id="name" name="name" value="<%= game.name %>" required>
                </div>
                <div class="mb-3">
                    <label for="release_year" class="form-label">Release Year</label>
                    <input type="number" class="form-control" id="release_year" name="release_year" value="<%= game.release_date_year %>" required>
                </div>
                <div class="mb-3">
                    <label for="price" class="form-label">Price</label>
                    <input type="text" class="form-control" id="price" name="price" value="<%= game.price %>" required>
                </div>
                <div class="mb-3">
                    <label for="windows" class="form-label">Windows Support</label>
                    <select class="form-control" id="windows" name="windows" required>
                        <option value="1" <%= game.windows ? 'selected' : '' %>>Yes</option>
                        <option value="0" <%= !game.windows ? 'selected' : '' %>>No</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label for="mac" class="form-label">Mac Support</label>
                    <select class="form-control" id="mac" name="mac" required>
                        <option value="1" <%= game.mac ? 'selected' : '' %>>Yes</option>
                        <option value="0" <%= !game.mac ? 'selected' : '' %>>No</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label for="linux" class="form-label">Linux Support</label>
                    <select class="form-control" id="linux" name="linux" required>
                        <option value="1" <%= game.linux ? 'selected' : '' %>>Yes</option>
                        <option value="0" <%= !game.linux ? 'selected' : '' %>>No</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label for="metacritic_score" class="form-label">Price</label>
                    <input type="text" class="form-control" id="metacritic_score" name="metacritic_score" value="<%= game.metacritic_score %>" required>
                </div>
                <button type="submit" class="btn btn-primary">Save Changes</button>
            </form>
        </div>
    </div>
</body>

</html>
